"use client"

/**
 * GA4 pageviews are intentionally handled by gtag auto page_view in RootLayout.
 * Keep this component as a no-op placeholder to avoid breaking layout imports.
 */
export function AnalyticsTracker() {
  return null
}
